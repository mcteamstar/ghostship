# TRN-160 Tasks: files.py streaming transfer tests

- [x] T1. Create `tests/unit/test_streaming_transfer.py` with the import bootstrap, helpers, and all test classes per `design.md`
- [x] T2. Run `bash tests/run.sh --unit` — confirm all new tests pass with no regressions
- [x] T3. Commit: `test(trn-160): add _ResponseChunkReader, _TarMemberStream, _transfer_upload slash-ref tests`
