"""Smoke placeholder proving that the E2E category is wired for discovery."""

import unittest


class E2ECategoryPlaceholderTests(unittest.TestCase):
    """Keep the E2E category executable until live-flow tests are added."""

    def test_e2e_category_is_discoverable(self) -> None:
        self.assertTrue(True)


if __name__ == "__main__":
    unittest.main()
