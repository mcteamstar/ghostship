mcp[cli]==2.2.0
httpx2==2.12.0
websockets>=13.0,<15.0
croniter==6.2.4
# Declared explicitly — mcp[cli] pulls these as transitive deps but pinning
# prevents silent breakage if mcp ever restructures its extras.
starlette>=1.0.1,<2.0.0
uvicorn>=0.31.1,<1.0.0
pytest>=8.0.0,<9.0.0  # test runner only
pytest-xdist>=3.5.0,<4.0.0  # test runner only
# TRN-136: captain.py imports cryptography directly for Ed25519 Admiral-mail
# signing. Declared explicitly rather than relied on transitively (PyJWT[crypto]
# happens to pull it today) so a change to that extra cannot silently break
# signing. Ranged, not exact: an exact old pin would downgrade the resolved
# version and strip security fixes from a security-critical library.
cryptography>=43.0.1,<51.0.0
